version: "3.8"
services:
  radar_rviz:
    build:
      context: ..
      dockerfile: docker/perception/radar_object_detection.Dockerfile
      cache_from: 
        - "${RADAR_OBJECT_DETECTION_IMAGE:?}:${CACHE_FROM_TAG}"
        - "${RADAR_OBJECT_DETECTION_IMAGE:?}:develop"
    image: "${RADAR_OBJECT_DETECTION_IMAGE:?}:${TAG}"
    user: ${FIXUID:?}:${FIXGID:?}
    volumes:
      - ../src/perception/radar_object_detection/radar_rviz:/home/docker/ament_ws/src/radar_rviz