ros2 pkg create --build-type ament_python lane_detection
cd /root
ls
ros2 pkg create --build-type ament_python lane_detection
cd /root
ls
rm -rf lane_detection/
cd /root/
os2 ros2 pkg create --build-type ament_cmake lane_detectionls
ls
os2 ros2 pkg create --build-type ament_cmake lane_detection
ros2 ros2 pkg create --build-type ament_cmake lane_detection
ros2 pkg create --build-type ament_cmake lane_detection
sudo chmod 777 lane_detection/
sudo chown users ./
ls
ls -la
sudo chown user .
sudo chown users:users
sudo chown users:users .
sudo chown watonomous lane_detection/
sudo chown users:watonomous lane_detection/
sudo chown watonomous:users lane_detection/
sudo chown watonomous lane_detection/
chmod -R a+rwX lane_detection/
ls
ls -la
chmod -R lane_detection/
chmod 777 -R lane_detection/ 
ls -la
ls
cd lane_detection/
colcon build --packages-select lane_detection
colcon build --packages-select lane_detection
. install/setup.bash
ros2 run lane_detection lane_detection_node
ros2 run lane_detection 
ros2 run lane_detection lane_detection
colcon build --packages-select lane_detection
ros2 run lane_detection lane_detection
