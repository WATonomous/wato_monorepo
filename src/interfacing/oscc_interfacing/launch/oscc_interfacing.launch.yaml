launch:
  # Arguments
  - arg:
      name: "config_file"
      default: "$(find-pkg-share oscc_interfacing)/config/oscc_interfacing_config.yaml"
      description: "Path to the OSCC interfacing configuration file"

  # Oscc node
  - node:
      pkg: "oscc_interfacing"
      exec: "oscc_interfacing_node"
      name: "oscc_interfacing_node"
      namespace: ""
      param:
        - from: "$(var config_file)"
