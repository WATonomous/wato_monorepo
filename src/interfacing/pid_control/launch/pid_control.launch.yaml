launch:
  - node:
      pkg: "pid_control"
      exec: "pid_control_node"
      name: "pid_control_node"
      output: "screen"
      param:
        - from: "$(find-pkg-share pid_control)/config/pid_control.yaml"
