name: Docker test

inputs:
  image:
    description: "the image to be tested"
    required: true

runs: 
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/test_image.sh
      shell: bash
      env:
        IMAGE: ${{ inputs.image }}
